---
description: 全局通用规则：中文回复、代码变更本地提交、遵循 Karpathy 编码准则
globs:
alwaysApply: true
---

# 全局规则（Global Rule）

以下规则始终生效，适用于本仓库中的所有对话与代码变更。

## 1. 使用中文回复

- 与用户交流时一律使用中文（除非用户明确要求其他语言）。
- 面向用户的解释、总结、说明均使用中文。
- 代码、命令、标识符及新增/修改的代码注释仍使用英文（遵循仓库 `AGENTS.md` 约定）。

## 2. 每次代码变更都本地提交

- 每完成一处逻辑上独立的代码变更，都要在本地执行 `git commit`。
- 提交遵循“一个逻辑变更一个提交”，不要把无关改动合并进同一个提交。
- 提交信息（commit message）必须完善，使用英文，建议采用如下结构：
  - 首行：`<type>: <简明摘要>`（type 取 `fix / feat / refactor / docs / chore / test`）。
  - 空一行后写正文：说明变更背景、动机、涉及范围与验证方式。
  - 若修复了 issue，注明 `Fixes #xxx` / `Closes #xxx`。
- 提交信息中不得添加 `Co-Authored-By`。
- 提交前至少做语法检查（如 `python -m py_compile`）或运行对应测试。

## 3. 严格遵守 Karpathy 编码准则

严格遵循 [andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills) 的要求：

### 3.1 先思考，再编码（Think Before Coding）
- 明确写出你的假设；不确定就先提问，不要静默臆测。
- 若存在多种理解，逐一列出，不要私自选择其一。
- 若有更简单的方案，主动指出并在必要时提出反对意见。
- 遇到不清楚的地方，先停下，指明困惑点并提问。

### 3.2 简单优先（Simplicity First）
- 只写解决问题所需的最小代码，不做任何投机性设计。
- 不实现超出需求的功能，不为一次性代码引入抽象。
- 不添加未被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景写错误处理。
- 若 200 行能压缩到 50 行，就重写。

### 3.3 外科手术式变更（Surgical Changes）
- 只改必须改的部分，只清理自己造成的“垃圾”。
- 不顺手“优化”相邻代码、注释或格式。
- 不重构没有坏掉的东西。
- 匹配现有代码风格，即使你有不同偏好。
- 发现无关的死代码只提示、不删除（除非被要求）。
- 仅移除因你的改动而产生的未使用的 import/变量/函数。
- 检验标准：每一行改动都能直接追溯到用户的需求。

### 3.4 目标驱动执行（Goal-Driven Execution）
- 将任务转化为可验证的成功标准，并循环执行直到通过验证：
  - “加校验” → “为非法输入写测试，然后让其通过”。
  - “修 bug” → “先写能复现的测试，然后让其通过”。
  - “重构 X” → “确保重构前后测试都通过”。
- 多步骤任务先给出简要计划，并为每步写明验证方式：
  1. [步骤] → 验证：[检查项]
  2. [步骤] → 验证：[检查项]

> 对于琐碎任务，可酌情灵活处理；非琐碎工作需完整遵循以上准则。
